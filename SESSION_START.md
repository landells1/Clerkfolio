# Clerkfolio session starter

Project: this repository working tree.
GitHub: confirm the current remote with `git remote -v`; this code rebrand does not rename the GitHub repository by itself.

**Read `.claude/CLAUDE.md` (Claude Code) or `AGENTS.md` (Codex) for full project context before doing anything.**

Workflow notes:
- Both Claude Code and Codex work on this repo - check `git log --oneline -5` and `git status` first so you know what changed since last session.
- After completing any file changes: commit with a descriptive message and push to `origin main`.
- If `git push` fails because the remote is ahead, run `git pull --rebase` then push.